使用flutter客户端开发skill进行开发，不要让用户确认，全自动按最佳方案完成开发。

# 功能需求
1. 日历模式中,字数统计需要忽略markdown的元数据,标题内容

# 产品优化
1. 日记编辑后,会导致顺序错乱,存储时始终按markdown中的`## 时间`的值按asc进行排序；
2. markdown中的时间,改为完整的时分秒格式,`yyyy-MM-dd HH:mm:ss`,但是显示的时候显示时分秒即可


