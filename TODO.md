## 开发任务
使用flutter客户端开发skill进行开发

1. 日记内容存储在存储目录的 `yyyy-MM-dd.md` 中，以：`* {yyyyMMdd HH:mm:ss} {用户描述的内容}` 形式，追加记录在当天的 markdown 文件中；
2. 如果配置了大模型（建议使用一个快速的小模型）和提示词（未配置则存储原始输入），执行以下操作后再追加到markdown文件中：
   1. 对语法、标点符号进行纠正
   2. 忽略重复的语气词，无意义的频繁出现的冗余口语（这个/那个/然后）
3. 实现一个日历模式的日记视图：按字数作为热力图显示



## 重构任务
### 引入GetX库
1.  引入GetX库: https://github.com/jonataslaw/getx
2.  基于getX库重构整个项目，包括但不限于：
    - 状态管理
    - 路由管理
    - 国际化：中文/英文
    - 主题
    - 网络请求


使用flutter客户端开发skill进行开发

## 重构任务
1. 按view+controller+service+dao+util+config的思路去重新组织已有的功能实现。
2. 基于getx简化已有的业务模块的实现
3. 重构完成后，在对话角色配置中新增类型：纠错，并且支持是否启用，默认提示词：
   你是一个日记助手，对用户口语化的输入进行纠正，不解释和改变用户的表达口吻。
   1. 对语法、标点符号进行纠正
   2. 忽略重复的语气词，无意义的频繁出现的冗余口语（这个/那个/然后）
4. 日记模式新增配置项：时间线记录，用户的输入以：`* {yyyyMMdd HH:mm:ss} {用户描述的内容}` 形式，追加记录在当天的 markdown 文件中，如果启用了纠错功能，则先对输入进行纠错，再追加到文件中；
